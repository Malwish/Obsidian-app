La función lineal es una función que gráficamente tiene forma de una recta.

Sea f : R→R (Una función de [[Dominio]] Real y [[Codominio o imagen]] [[Reales]])
Una **función lineal (afín)** es una función de la forma:

f(x)=mx+b

donde m,b ∈ R.

m= [[Pendiente]]
b = [[Ordenada al origen]]


