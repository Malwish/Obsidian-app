Para calcular la intersección de una función F(x) con otra G(x) sin analizarlas gráficamente podemos igualarlas de modo que podamos calcular los valores para los que ambas funciones sean iguales.

F(x) = G(x) cuando sus valores sean los mismos. Por tanto será la intersección de ambas.