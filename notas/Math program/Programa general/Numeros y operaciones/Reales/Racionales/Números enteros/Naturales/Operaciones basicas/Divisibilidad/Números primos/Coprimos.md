Dos números son primos entre sí o coprimos si y solo sí tienen como único factor común o divisor común el 1. Equivalente podemos decir que su único [[Máximo común divisor]] es 1.

Son coprimos: el 15 y el 22.

Podemos verificarlos fácilmente si usamos la [[Factorización en base a números primos.]] para reducirlos y ver que no comparten [[Múltiplos]].

3.5 -> 11.2

